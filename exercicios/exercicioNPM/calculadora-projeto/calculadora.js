

export const soma = (numA, numB) => {
 return numA + numB
}

export const subtracao = (numA, numB) => {
    return numA - numB
}

export const multiplicacao = (numA, numB) => {
    return numA * numB
}

export const dividir = (numA, numB) => {
    return numA / numB
}
